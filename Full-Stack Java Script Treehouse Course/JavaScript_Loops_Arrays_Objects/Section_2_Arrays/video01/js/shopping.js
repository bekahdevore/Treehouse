/*array = way of stroring multiple variables
var = way of stroring single variables, flexible 
(single number, don't have to know how many items ahead of time you want to store)

	var nameArray = [Comma, separated, list, of, items]
*/

var shoppingList = [
	'carrots',
	'milk',
	'eggs'
];
console.log(shoppingList[0])
console.log(shoppingList[1])
console.log(shoppingList[2])
console.log(shoppingList[3])
